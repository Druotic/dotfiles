# Default environment variables for zsh configuration
# These can be overridden by creating a .env file in the same directory

COMPANY="replace me"
DEFAULT_REPO_NAME="replace me"
PATH_TO_MAIN_SERVICE="$DEFAULT_REPO_NAME/replace me"
